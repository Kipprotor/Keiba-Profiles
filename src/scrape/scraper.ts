import {
  extractPrize,
  scrapeHorseInfo,
  scrapeHorseTitle,
  scrapePedigree,
  scrapeProfTable,
} from "./scrapeProfile.ts";
import {
  horseInfo2SearchResult,
  scrapeSearchResult,
} from "./scrapeSearchResult.ts";

export {
  extractPrize,
  horseInfo2SearchResult,
  scrapeHorseInfo,
  scrapeHorseTitle,
  scrapePedigree,
  scrapeProfTable,
  scrapeSearchResult,
};
