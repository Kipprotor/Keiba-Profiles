import {
  scrapeHorseInfo,
  scrapeHorseTitle,
  scrapePedigree,
  scrapeProfTable,
} from "./scrapeProfile.ts";
import { scrapeSearchResult } from "./scrapeSearchResult.ts";

export {
  scrapeHorseInfo,
  scrapeHorseTitle,
  scrapePedigree,
  scrapeProfTable,
  scrapeSearchResult,
};
